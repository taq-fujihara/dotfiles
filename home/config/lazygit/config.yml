git:
  branchLogCmd: "git graph"
  diffRenderers:
    - colorArg: always
      command: delta --paging=never --features="theme-nord view-unified"
    - colorArg: always
      command: delta --paging=never --features="theme-nord view-side-by-side"
